<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBarBlue">
        <item name="colorPrimary">@color/colorBlue2</item>
        <item name="colorPrimaryDark">@color/colorBlue1</item>
        <item name="colorAccent">@color/colorBlue2</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:textColorHint">@color/colorWhite</item>
    </style>

    <style name="AppTheme.NoActionBarGray">
        <item name="colorPrimary">@color/colorCream</item>
        <item name="colorPrimaryDark">@color/colorGray</item>
        <item name="colorAccent">@color/colorDarkGray</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="WhiteTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorCream</item>
        <item name="colorPrimaryDark">@color/colorBlue</item>
        <item name="colorAccent">@color/colorGreenPantone</item>
        <item name="colorControlNormal">@color/colorCream</item>
        <item name="colorControlActivated">@color/colorCream</item>
        <item name="colorControlHighlight">@color/colorCream</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SwitchCompatTheme" >
        <item name="colorControlActivated">#f00</item>
    </style>
    <style name="SwitchCompatStyle" >
        <item name="android:thumb">@drawable/switch_hotspot</item>
        <item name="trackTint">@color/switch_color</item>
    </style>

</resources>
